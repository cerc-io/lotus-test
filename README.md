# lotus-test

## Run Tests

* Install dependencies and compile contracts:

```bash
yarn
yarn compile
```

* Create an `.env` file:

```bash
cp .env.sample .env
# Fill in .env
```

* Run tests:

```bash
yarn test
```
